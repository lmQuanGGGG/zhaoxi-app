# ZhaoXi Platform 18.0.3 — Native Mobile Interaction Layer

Synchronized Customer + Partner + Admin release.

Phone-native interaction standards:
- Legacy operational tables render as local card rows instead of widening the page.
- Shared filter trigger + bottom/full screen sheet primitives.
- Shared sticky mobile action bar.
- Detail/modal presentation becomes full-screen on phone and centered sheet on tablet/desktop.
- Customer, Partner and Admin inherit the same interaction system and UI tokens.
- Existing API/business logic remains unchanged.
