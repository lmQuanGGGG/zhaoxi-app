import fs from "node:fs";const req=["packages/beta-access/package.json","packages/beta-access/index.tsx","apps/admin/app/beta-access/page.tsx","apps/admin/app/api/platform-beta-access/admin/route.ts"];for(const f of req)if(!fs.existsSync(f))throw new Error(`Missing ${f}`);for(const a of ["customer","partner","driver","admin"]){const p=JSON.parse(fs.readFileSync(`apps/${a}/package.json`,`utf8`));if(p.dependencies?.["@zhaoxi/beta-access"]!=="15.3.0")throw new Error(`${a} beta access dependency mismatch`);for(const r of ["check","redeem"])if(!fs.existsSync(`apps/${a}/app/api/platform-beta-access/${r}/route.ts`))throw new Error(`Missing ${a} ${r}`)}console.log("Sprint 15.3 Platform Beta Access Control & Invite System structure is valid.");
