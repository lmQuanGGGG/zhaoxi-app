import fs from 'node:fs';const p=JSON.parse(fs.readFileSync('package.json','utf8'));if(p.version!=='16.12.1')throw new Error('Platform version mismatch');const auth=fs.readFileSync('packages/auth/src/index.tsx','utf8');for(const x of ['ZhaoXiQrLogin','AdminCardLogin','/api/auth/unified/qr/session','authMethod?: "account" | "wechat" | "qr" | "internal"'])if(!auth.includes(x))throw new Error(`Missing ${x}`);if(auth.includes('function submit() {\n    if (!valid) return;'))throw new Error('Legacy account bypass remains');console.log('Sprint 16.12.1 Platform QR-only Customer/Partner and Admin Card structure is valid.');
