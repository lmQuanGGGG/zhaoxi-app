import fs from "node:fs";const apps=["customer","partner","driver","admin"];const files=["packages/feature-flags/index.tsx","packages/feature-flags/package.json","apps/admin/app/feature-flags/page.tsx",...apps.map(a=>`apps/${a}/app/api/platform-feature-flags/route.ts`),"SPRINT_15_8.md"];for(const f of files)if(!fs.existsSync(f))throw new Error(`Missing ${f}`);for(const a of apps){const p=JSON.parse(fs.readFileSync(`apps/${a}/package.json`,"utf8"));if(p.dependencies["@zhaoxi/feature-flags"]!=="15.8.0")throw new Error(`${a} feature-flags dependency invalid`)}const p=JSON.parse(fs.readFileSync("package.json","utf8"));if(p.version!=="15.8.0")throw new Error("Platform version must be 15.8.0");console.log("Sprint 15.8 Platform Feature Flags & Progressive Rollout Control structure is valid.");