# Sprint 15.6 — Secure Role Switch & Cross-App Session Handoff

Account Center can switch among provisioned roles. The handoff code is short-lived, single-use, and carried in the URL fragment so it is not sent in HTTP referrers.
