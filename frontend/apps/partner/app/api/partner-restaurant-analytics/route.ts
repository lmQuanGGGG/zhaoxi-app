import {NextRequest} from "next/server";export const dynamic="force-dynamic";const backend=()=>process.env.ZHAOXI_BACKEND_URL||process.env.NEXT_PUBLIC_ZHAOXI_API_URL||"https://zhaoxi-backend.vercel.app";
export async function GET(r:NextRequest){const token=r.cookies.get("zx_access_v2")?.value;const headers:Record<string,string>={};if(token)headers.authorization=`Bearer ${token}`;try{const u=await fetch(`${backend()}/api/partner-restaurant-analytics?${r.nextUrl.searchParams}`,{headers,cache:"no-store"});return Response.json(await u.json(),{status:u.status,headers:{"cache-control":"no-store"}})}catch{return Response.json({ok:false,error:{code:"RESTAURANT_ANALYTICS_UNAVAILABLE"}},{status:503})}}
