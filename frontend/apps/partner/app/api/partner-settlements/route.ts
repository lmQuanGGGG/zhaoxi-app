import{NextRequest}from"next/server";const backend=()=>process.env.ZHAOXI_BACKEND_URL||process.env.NEXT_PUBLIC_ZHAOXI_API_URL||"https://zhaoxi-backend.vercel.app";export async function GET(r:NextRequest){const h:Record<string,string>={};const t=r.cookies.get("zx_access_v2")?.value;if(t)h.authorization=`Bearer ${t}`;const u=await fetch(`${backend()}/api/partner-settlements?${r.nextUrl.searchParams}`,{headers:h,cache:"no-store"});return Response.json(await u.json(),{status:u.status})}