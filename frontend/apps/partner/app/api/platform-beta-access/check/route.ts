async function safeJsonResponse(res:Response){try{const text=await res.text();return Response.json(JSON.parse(text),{status:res.status});}catch{return Response.json({ok:false,error:{code:"UPSTREAM_UNAVAILABLE",upstreamStatus:res.status}},{status:res.status>=400?res.status:502});}}
import {NextRequest} from "next/server";export const dynamic="force-dynamic";const backend=()=>process.env.ZHAOXI_BACKEND_URL||process.env.NEXT_PUBLIC_ZHAOXI_API_URL||"https://zhaoxi-app-puce.vercel.app";const access=(r:NextRequest)=>r.cookies.get("zx_access_v2")?.value||"";export async function GET(r:NextRequest){const a=access(r);const u=await fetch(`${backend()}/api/beta-access/check`,{cache:"no-store",headers:a?{authorization:`Bearer ${a}`}:{}});return safeJsonResponse(u)}
