import { NextRequest } from "next/server";
export const dynamic="force-dynamic";
const backend=()=>process.env.ZHAOXI_BACKEND_URL||process.env.NEXT_PUBLIC_ZHAOXI_API_URL||"https://zhaoxi-app-puce.vercel.app";
function auth(request:NextRequest):Record<string,string>{const token=request.cookies.get("zx_access_v2")?.value;return token?{authorization:`Bearer ${token}`}:{}}
export async function GET(request:NextRequest){try{const params=new URLSearchParams(request.nextUrl.searchParams);const response=await fetch(`${backend()}/api/services?${params}`,{headers:auth(request),cache:"no-store"});const payload=await response.json().catch(()=>null);return Response.json(payload||{ok:true,data:[]},{status:response.status})}catch{return Response.json({ok:false,data:[],error:{code:"SERVICES_UNAVAILABLE"}},{status:503})}}
export async function POST(request:NextRequest){try{const response=await fetch(`${backend()}/api/services`,{method:"POST",headers:{"content-type":"application/json",...auth(request)},body:await request.text(),cache:"no-store"});const payload=await response.json().catch(()=>null);return Response.json(payload||{ok:false,error:{code:"SERVICE_CREATE_FAILED",message:"Không thể thêm món."}},{status:response.status})}catch{return Response.json({ok:false,error:{code:"SERVICE_CREATE_UNAVAILABLE",message:"Không thể kết nối máy chủ để thêm món."}},{status:503})}}
