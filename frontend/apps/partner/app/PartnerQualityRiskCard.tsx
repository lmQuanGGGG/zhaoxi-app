"use client";import{useEffect,useState}from"react";import{useZhaoXiLocale}from"@zhaoxi/i18n";const C={"zh-CN":{title:"质量风险状态",risk:"风险等级",score:"质量分",action:"建议措施",flags:"需要改进",safe:"当前无风险标记",note:"风险状态仅用于运营质量，不影响合作伙伴资金。"},"zh-TW":{title:"品質風險狀態",risk:"風險等級",score:"品質分",action:"建議措施",flags:"需要改善",safe:"目前無風險標記",note:"風險狀態僅用於營運品質，不影響合作夥伴資金。"},"vi-VN":{title:"Trạng thái rủi ro chất lượng",risk:"Mức rủi ro",score:"Quality Score",action:"Khuyến nghị",flags:"Cần cải thiện",safe:"Hiện không có risk flag",note:"Risk chỉ phục vụ chất lượng vận hành, không ảnh hưởng tiền của Partner."},"en-US":{title:"Quality risk status",risk:"Risk level",score:"Quality Score",action:"Recommended action",flags:"Needs improvement",safe:"No current risk flags",note:"Risk status is operational only and does not affect Partner funds."}}as const;export default function PartnerQualityRiskCard({organizationId}:{organizationId:string}){const{locale}=useZhaoXiLocale(),t=C[locale];const[data,setData]=useState<any>(null);useEffect(()=>{if(!organizationId)return;fetch(`/api/partner-quality?organizationId=${organizationId}`,{cache:"no-store"}).then(r=>r.json()).then(j=>j?.ok&&setData(j.data))},[organizationId]);if(!data)return null;return <section style={{padding:10,border:"1px solid #dfe8e3",borderRadius:14,background:"#fff",margin:"10px 0"}}><b>🛡 {t.title}</b><div style={{display:"flex",justifyContent:"space-between",marginTop:5}}><span>{t.risk}: <strong>{String(data.risk.riskLevel).toUpperCase()}</strong></span><strong>{t.score}: {data.summary.qualityScore}</strong></div><small style={{display:"block",marginTop:3}}>{t.action}: {data.risk.recommendedAction}</small><small style={{display:"block",color:"#067647",marginTop:3}}>✓ {t.note}</small><div style={{marginTop:5}}>{data.risk.flags?.length?data.risk.flags.map((f:any)=><small key={f.code} style={{display:"block",color:"#b54708"}}>• {f.code}</small>):<small>{t.safe}</small>}</div></section>}
