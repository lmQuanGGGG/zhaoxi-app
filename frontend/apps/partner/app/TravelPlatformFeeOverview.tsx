"use client";
import{useEffect,useState}from"react";import{useZhaoXiLocale}from"@zhaoxi/i18n";
const C={"zh-CN":{title:"平台使用费",direct:"客户付款直接进入您的账户",due:"待付赵喜平台费",paid:"已付平台费",mode:"当前费率"},"zh-TW":{title:"平台使用費",direct:"客戶付款直接進入您的帳戶",due:"待付趙喜平台費",paid:"已付平台費",mode:"目前費率"},"vi-VN":{title:"Phí sử dụng nền tảng",direct:"Tiền Customer thanh toán trực tiếp vào tài khoản Partner",due:"Phí ZhaoXi đang phải trả",paid:"Phí đã trả",mode:"Mức phí hiện tại"},"en-US":{title:"Platform usage fee",direct:"Customer payments go directly to your Partner account",due:"ZhaoXi fee due",paid:"Fee paid",mode:"Current fee"}} as const;
export default function TravelPlatformFeeOverview({organizationId}:{organizationId:string}){const{locale}=useZhaoXiLocale(),t=C[locale];const[data,setData]=useState<any>(null);useEffect(()=>{if(!organizationId)return;fetch(`/api/partner-travel-fees?organizationId=${organizationId}`,{cache:"no-store"}).then(r=>r.json().catch(()=>null)).then(j=>j?.ok&&setData(j.data)).catch(()=>{})},[organizationId]);if(!data)return null;const p=data.policy,s=data.ledger.summary;return <section style={{padding:10,border:"1px solid #dfe8e3",borderRadius:14,background:"#fff",margin:"10px 0"}}><b>💳 {t.title}</b><small style={{display:"block",color:"#067647",marginTop:4}}>✓ {t.direct}</small><div style={{display:"grid",gridTemplateColumns:"repeat(3,minmax(0,1fr))",gap:6,marginTop:7,fontSize:18}}><div>{t.mode}<b style={{display:"block"}}>{p.enabled?`${p.mode} · ${(Number(p.percentageBps||0)/100).toFixed(2)}% · ${Number(p.fixedPerBooking||0).toLocaleString("vi-VN")} VND`:"OFF"}</b></div><div>{t.due}<b style={{display:"block"}}>{Number(s.platformFeeDue||0).toLocaleString("vi-VN")} VND</b></div><div>{t.paid}<b style={{display:"block"}}>{Number(s.platformFeePaid||0).toLocaleString("vi-VN")} VND</b></div></div></section>}
