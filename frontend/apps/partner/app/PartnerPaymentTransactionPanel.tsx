"use client";
import{useEffect,useState}from"react";import{useZhaoXiLocale}from"@zhaoxi/i18n";
const C={"zh-CN":{title:"支付交易记录",direct:"客户资金直接进入您的商户账户",empty:"暂无交易",status:"状态",amount:"金额",source:"来源",reference:"网关参考号"},"zh-TW":{title:"支付交易記錄",direct:"客戶資金直接進入您的商戶帳戶",empty:"暫無交易",status:"狀態",amount:"金額",source:"來源",reference:"閘道參考號"},"vi-VN":{title:"Nhật ký giao dịch thanh toán",direct:"Tiền Customer đi trực tiếp vào merchant của Partner",empty:"Chưa có giao dịch",status:"Trạng thái",amount:"Số tiền",source:"Nguồn",reference:"Mã tham chiếu gateway"},"en-US":{title:"Payment transaction log",direct:"Customer funds go directly to your Partner merchant",empty:"No transactions yet",status:"Status",amount:"Amount",source:"Source",reference:"Gateway reference"}} as const;
export default function PartnerPaymentTransactionPanel({organizationId}:{organizationId:string}){const{locale}=useZhaoXiLocale(),t=C[locale];const[rows,setRows]=useState<any[]>([]);useEffect(()=>{if(!organizationId)return;fetch(`/api/partner-payment-transactions?organizationId=${organizationId}`,{cache:"no-store"}).then(r=>r.json().catch(()=>null)).then(j=>j?.ok&&setRows(j.data||[])).catch(()=>{})},[organizationId]);return <section style={{padding:10,border:"1px solid #dfe8e3",borderRadius:14,background:"#fff",margin:"10px 0"}}><b>🧾 {t.title}</b><small style={{display:"block",color:"#067647",marginTop:3}}>✓ {t.direct}</small>{!rows.length?<small style={{display:"block",marginTop:6,color:"#64748b"}}>{t.empty}</small>:<div style={{display:"grid",gap:5,marginTop:7,maxHeight:280,overflowY:"auto"}}>{rows.slice(0,100).map(x=><div key={x.id} style={{padding:7,border:"1px solid #edf1ef",borderRadius:9,fontSize:18}}><b>{x.intentId}</b><span style={{float:"right",fontWeight:900}}>{x.status}</span><small style={{display:"block"}}>{t.amount}: {Number(x.amount||0).toLocaleString("vi-VN")} {x.currency} · {t.source}: {x.source}</small>{x.providerReference&&<small style={{display:"block"}}>{t.reference}: {x.providerReference}</small>}</div>)}</div>}</section>}
