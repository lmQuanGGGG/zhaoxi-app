"use client";
import{useEffect,useState}from"react";import{useZhaoXiLocale}from"@zhaoxi/i18n";
const C={"zh-CN":{title:"支付支持质量",score:"质量分",tickets:"工单",active:"处理中",overdue:"超 SLA",sla:"SLA 达标",refund:"退款 ETA 达标",response:"首次响应",escalation:"升级率",exceptions:"异常率",min:"分钟",sample:"样本"},"zh-TW":{title:"支付支援品質",score:"品質分",tickets:"工單",active:"處理中",overdue:"超 SLA",sla:"SLA 達標",refund:"退款 ETA 達標",response:"首次回應",escalation:"升級率",exceptions:"異常率",min:"分鐘",sample:"樣本"},"vi-VN":{title:"Chất lượng hỗ trợ thanh toán",score:"Quality Score",tickets:"Ticket",active:"Đang xử lý",overdue:"Quá SLA",sla:"Đúng SLA",refund:"Refund đúng ETA",response:"Phản hồi đầu tiên",escalation:"Tỷ lệ escalation",exceptions:"Tỷ lệ exception",min:"phút",sample:"mẫu"},"en-US":{title:"Payment support quality",score:"Quality Score",tickets:"Tickets",active:"Active",overdue:"SLA overdue",sla:"SLA met",refund:"Refund ETA met",response:"First response",escalation:"Escalation rate",exceptions:"Exception rate",min:"min",sample:"sample"}}as const;
export default function PartnerPaymentSupportAnalytics({organizationId}:{organizationId:string}){const{locale}=useZhaoXiLocale(),t=C[locale];const[data,setData]=useState<any>(null);useEffect(()=>{if(!organizationId)return;fetch(`/api/partner-payment-support-analytics?organizationId=${organizationId}`,{cache:"no-store"}).then(r=>r.json()).then(j=>j?.ok&&setData(j.data))},[organizationId]);if(!data)return null;const s=data.summary;return <section style={card}><div style={{display:"flex",justifyContent:"space-between",alignItems:"center"}}><div><b>📈 {t.title}</b><small style={{display:"block",color:"#64748b"}}>{t.response}: {s.firstResponseAvgMinutes} {t.min}</small></div><strong style={{fontSize:24,color:s.qualityScore>=90?"#067647":s.qualityScore>=75?"#b54708":"#b42318"}}>{s.qualityScore}</strong></div><div style={grid}><M l={t.tickets} v={s.ticketCount}/><M l={t.active} v={s.active}/><M l={t.overdue} v={s.overdue}/><M l={t.sla} v={`${s.slaMetRate}% · ${s.slaSampleSize} ${t.sample}`}/><M l={t.refund} v={`${s.refundEtaMetRate}% · ${s.refundEtaSampleSize} ${t.sample}`}/><M l={t.escalation} v={`${s.escalationRate}%`}/><M l={t.exceptions} v={`${s.exceptionRate}%`}/></div></section>}
function M({l,v}:{l:string;v:string|number}){return <div style={{padding:7,border:"1px solid #edf1ef",borderRadius:9}}><small style={{display:"block",fontSize:18,color:"#64748b"}}>{l}</small><b style={{fontSize:18}}>{v}</b></div>}const card={padding:10,border:"1px solid #dfe8e3",borderRadius:14,background:"#fff",margin:"10px 0"}as const,grid={display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(100px,1fr))",gap:5,marginTop:8}as const;
