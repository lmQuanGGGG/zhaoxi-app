import {NextRequest} from "next/server";const backend=()=>process.env.ZHAOXI_BACKEND_URL||process.env.NEXT_PUBLIC_ZHAOXI_API_URL||"https://zhaoxi-backend.vercel.app";function h(r:NextRequest):Record<string,string>{const a=r.cookies.get("zx_access_v2")?.value;return a?{authorization:`Bearer ${a}`}:{}}export async function GET(r:NextRequest){const u=await fetch(`${backend()}/api/onboarding/admin`,{cache:"no-store",headers:h(r)});return Response.json(await u.json(),{status:u.status})}
