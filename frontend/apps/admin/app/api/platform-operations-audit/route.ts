import {NextRequest} from "next/server";const backend=()=>process.env.ZHAOXI_BACKEND_URL||process.env.NEXT_PUBLIC_ZHAOXI_API_URL||"https://zhaoxi-app-puce.vercel.app";function h(r:NextRequest):Record<string,string>{const x:Record<string,string>={};const a=r.cookies.get("zx_access_v2")?.value;if(a)x.authorization=`Bearer ${a}`;return x}export async function GET(r:NextRequest){const u=new URL(r.url);const q=new URLSearchParams();const area=u.searchParams.get("area");const limit=u.searchParams.get("limit");if(area)q.set("area",area);if(limit)q.set("limit",limit);const upstream=await fetch(`${backend()}/api/operations-audit?${q.toString()}`,{cache:"no-store",headers:h(r)});return Response.json(await upstream.json(),{status:upstream.status,headers:{"cache-control":"no-store"}})}