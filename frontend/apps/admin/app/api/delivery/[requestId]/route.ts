import {NextRequest} from "next/server";export const dynamic="force-dynamic";const backend=()=>process.env.ZHAOXI_BACKEND_URL||process.env.NEXT_PUBLIC_ZHAOXI_API_URL||"https://zhaoxi-app-puce.vercel.app";export async function GET(request:NextRequest,context:{params:Promise<{requestId:string}>}){const{requestId}=await context.params,token=request.cookies.get("zx_access_v2")?.value;const r=await fetch(`${backend()}/api/delivery/${encodeURIComponent(requestId)}`,{headers:token?{authorization:`Bearer ${token}`}:{},cache:"no-store"});return Response.json(await r.json(),{status:r.status,headers:{"cache-control":"no-store"}});}
