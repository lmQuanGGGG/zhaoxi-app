import {NextRequest} from "next/server";const backend=()=>process.env.ZHAOXI_BACKEND_URL||process.env.NEXT_PUBLIC_ZHAOXI_API_URL||"https://zhaoxi-backend.vercel.app";function h(r:NextRequest):Record<string,string>{const x:Record<string,string>={};const a=r.cookies.get("zx_access_v2")?.value;if(a)x.authorization=`Bearer ${a}`;return x}export async function GET(r:NextRequest){const u=new URL(r.url);const q=new URLSearchParams();for(const k of ["resourceType","app","actorUserId","hours","limit"]){const v=u.searchParams.get(k);if(v)q.set(k,v)}const upstream=await fetch(`${backend()}/api/release-audit?${q.toString()}`,{cache:"no-store",headers:h(r)});return Response.json(await upstream.json(),{status:upstream.status,headers:{"cache-control":"no-store"}})}