import{NextRequest}from"next/server";export const dynamic="force-dynamic";const b=()=>process.env.ZHAOXI_BACKEND_URL||process.env.NEXT_PUBLIC_ZHAOXI_API_URL||"https://zhaoxi-backend.vercel.app";export async function GET(r:NextRequest){const t=r.cookies.get("zx_access_v2")?.value,h:Record<string,string>={};if(t)h.authorization=`Bearer ${t}`;const u=await fetch(`${b()}/api/admin-partner-payment-gateways`,{headers:h,cache:"no-store"});return Response.json(await u.json(),{status:u.status})}