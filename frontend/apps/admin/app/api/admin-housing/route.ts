import{NextRequest}from"next/server";export const dynamic="force-dynamic";const backend=()=>process.env.ZHAOXI_BACKEND_URL||process.env.NEXT_PUBLIC_ZHAOXI_API_URL||"https://zhaoxi-backend.vercel.app";export async function GET(r:NextRequest){try{const token=r.cookies.get("zx_access_v2")?.value,h:Record<string,string>={};if(token)h.authorization=`Bearer ${token}`;const u=await fetch(`${backend()}/api/admin-housing?${r.nextUrl.searchParams.toString()}`,{headers:h,cache:"no-store"});return Response.json(await u.json(),{status:u.status})}catch{return Response.json({ok:false,error:{code:"ADMIN_HOUSING_UNAVAILABLE"}},{status:503})}}