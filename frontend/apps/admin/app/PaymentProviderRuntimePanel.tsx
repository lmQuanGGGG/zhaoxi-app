"use client";
import{useEffect,useState}from"react";import{useZhaoXiLocale}from"@zhaoxi/i18n";
const C={"zh-CN":{title:"支付服务商运行状态",closed:"正常",open:"熔断",half:"试探恢复",fail:"连续失败",success:"成功",role:"角色",primary:"主通道",fallback:"备用通道",until:"恢复尝试时间"},"zh-TW":{title:"支付服務商運行狀態",closed:"正常",open:"熔斷",half:"試探恢復",fail:"連續失敗",success:"成功",role:"角色",primary:"主通道",fallback:"備用通道",until:"恢復嘗試時間"},"vi-VN":{title:"Runtime Health & Circuit Breaker",closed:"Bình thường",open:"Đang ngắt mạch",half:"Đang thử phục hồi",fail:"Lỗi liên tiếp",success:"Lần thành công",role:"Vai trò",primary:"Chính",fallback:"Dự phòng",until:"Thử lại sau"},"en-US":{title:"Runtime Health & Circuit Breaker",closed:"Healthy",open:"Circuit open",half:"Half-open probe",fail:"Consecutive failures",success:"Successes",role:"Role",primary:"Primary",fallback:"Fallback",until:"Retry after"}} as const;
export default function PaymentProviderRuntimePanel(){const{locale}=useZhaoXiLocale(),t=C[locale];const[data,setData]=useState<any>(null);useEffect(()=>{const load=()=>fetch("/api/admin-payment-provider-runtime",{cache:"no-store"}).then(r=>r.json()).then(j=>j?.ok&&setData(j.data));void load();const x=setInterval(load,12000);return()=>clearInterval(x)},[]);return <section style={card}><b>⚡ {t.title}</b><div style={{display:"grid",gap:6,marginTop:7}}>{(data?.rows||[]).map((x:any)=><div key={`${x.organization.id}-${x.provider.id}-${x.role}`} style={{padding:8,border:"1px solid #edf1ef",borderRadius:10,fontSize:8}}><b>{x.organization.name} · {x.provider.name}</b><span style={{float:"right",fontWeight:900,color:x.health.state==="closed"?"#067647":x.health.state==="open"?"#b42318":"#b54708"}}>{x.health.state==="closed"?t.closed:x.health.state==="open"?t.open:t.half}</span><small style={{display:"block"}}>{t.role}: {x.role==="primary"?t.primary:t.fallback} · {t.fail}: {x.health.consecutiveFailures} · {t.success}: {x.health.totalSuccess}</small>{x.health.openUntil&&<small style={{display:"block",color:"#b42318"}}>{t.until}: {new Date(x.health.openUntil).toLocaleString()}</small>}{x.health.lastError&&<small style={{display:"block",color:"#64748b"}}>{x.health.lastError}</small>}</div>)}</div></section>}
const card={padding:11,border:"1px solid #e3eae6",borderRadius:14,background:"#fff"} as const;
