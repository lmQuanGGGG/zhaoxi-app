"use client";import {useEffect,useState} from "react";type A={id:string;role:string;status:string;displayName:string|null;nickname:string|null;wechatOpenId:string|null;phone:string|null;businessName:string|null;businessType:string|null;addressText:string|null;vehicleType:string|null;plateNumber:string|null;notes:string|null;reviewNote:string|null};export default function AdminOnboarding(){const[rows,setRows]=useState<A[]>([]);async function load(){const r=await fetch("/api/platform-onboarding/admin",{cache:"no-store"});const p=await r.json();if(r.ok)setRows(p.data||[])}useEffect(()=>{void load()},[]);async function review(id:string,status:string){const note=status==="rejected"?prompt("Reason for rejection")||"":"Approved by ZhaoXi Admin";await fetch(`/api/platform-onboarding/admin/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status,reviewNote:note})});await load()}return <main style={{width:"min(100%,620px)",margin:"0 auto",minHeight:"100dvh",padding:18,background:"#f6faf7",fontFamily:"Inter,Arial,sans-serif"}}><small style={{color:"#07a856",fontWeight:800}}>ZHAOXI · BETA 15.4</small><h1>Onboarding & Role Provisioning</h1><p style={{color:"#64748b"}}>Review Partner and Driver applications. Approval provisions the role on the Backend; Partner approval also creates the operating organization.</p><section style={{display:"grid",gap:10}}>{rows.map(x=><article key={x.id} style={{background:"white",padding:14,borderRadius:16}}><div style={{display:"flex",justifyContent:"space-between",gap:8}}><b>{x.displayName||x.nickname||x.wechatOpenId||"WeChat user"}</b><span>{x.role} · {x.status}</span></div>{x.businessName&&<p><b>{x.businessName}</b> · {x.businessType}<br/>{x.addressText}</p>}{x.vehicleType&&<p>{x.vehicleType} · {x.plateNumber}</p>}{x.phone&&<p>{x.phone}</p>}{x.notes&&<small>{x.notes}</small>}{x.status==="submitted"&&<div style={{display:"flex",gap:8,marginTop:10}}><button onClick={()=>review(x.id,"approved")} style={{background:"#07c160",color:"white",border:0,borderRadius:10,padding:"9px 12px"}}>Approve</button><button onClick={()=>review(x.id,"rejected")} style={{borderRadius:10,padding:"9px 12px"}}>Reject</button></div>}{x.reviewNote&&<small style={{display:"block",marginTop:8,color:"#64748b"}}>{x.reviewNote}</small>}</article>)}</section></main>}
