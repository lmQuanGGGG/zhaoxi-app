"use client";import{useEffect,useState}from"react";import{useZhaoXiLocale}from"@zhaoxi/i18n";const C={"zh-CN":{title:"支付异常管理",empty:"暂无未处理异常",resolve:"标记已处理",note:"处理备注"},"zh-TW":{title:"支付異常管理",empty:"暫無未處理異常",resolve:"標記已處理",note:"處理備註"},"vi-VN":{title:"Payment Exception Management",empty:"Không có exception chưa xử lý",resolve:"Đánh dấu đã xử lý",note:"Ghi chú xử lý"},"en-US":{title:"Payment Exception Management",empty:"No unresolved exceptions",resolve:"Mark resolved",note:"Resolution note"}}as const;export default function PaymentExceptionPanel(){const{locale}=useZhaoXiLocale(),t=C[locale];const[rows,setRows]=useState<any[]>([]),[note,setNote]=useState<Record<string,string>>({});async function load(){const r=await fetch("/api/admin-payment-exceptions",{cache:"no-store"}),j=await r.json();if(j?.ok)setRows(j.data||[])}useEffect(()=>{void load()},[]);async function resolve(id:string){await fetch(`/api/admin-payment-exceptions/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({note:note[id]||""})});await load()}return <section className="zx-native-workspace" style={{padding:11,border:"1px solid #e3eae6",borderRadius:14,background:"#fff"}}><b>🚨 {t.title}</b>{!rows.length&&<p style={{fontSize:8}}>{t.empty}</p>}<div style={{display:"grid",gap:6,marginTop:6}}>{rows.map(x=><div key={x.requestId} style={{padding:8,border:"1px solid #fee4e2",borderRadius:9,fontSize:8}}><b>{x.requestCode} · {x.exception.type}</b><small style={{display:"block",color:"#b42318"}}>{x.exception.error}</small><input placeholder={t.note} value={note[x.requestId]||""} onChange={e=>setNote({...note,[x.requestId]:e.target.value})} style={{width:"100%",boxSizing:"border-box",margin:"5px 0",padding:5}}/><button onClick={()=>void resolve(x.requestId)}>{t.resolve}</button></div>)}</div></section>}
