import Link from "next/link";import styles from "../hub.module.css";
type Item={title:string;cn?:string;desc:string;meta?:string};type Props={icon:string;title:string;cn?:string;subtitle:string;items:Item[];cta:string;notice?:string};
export default function ServiceLanding({icon,title,subtitle,items,cta,notice}:Props){return <main className={styles.shell}><div className={styles.topbar}><Link className={styles.back} href="/">←</Link><h1 className={styles.pageTitle}>{icon} {title}</h1><p className={styles.subtitle}>{subtitle}</p></div><div className={styles.content}>{notice&&<div className={styles.notice}>{notice}</div>}<div className={styles.list}>{items.map((x,i)=><article className={styles.listing} key={i}><div className={styles.title}>{x.title}</div><div className={styles.desc}>{x.desc}</div>{x.meta&&<div style={{marginTop:10}}><span className={styles.badge}>{x.meta}</span></div>}</article>)}</div><div style={{marginTop:18}}><button className={styles.button}>{cta}</button></div></div></main>}
