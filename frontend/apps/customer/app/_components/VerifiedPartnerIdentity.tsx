"use client";import Link from"next/link";import{useEffect,useState}from"react";import{useZhaoXiLocale}from"@zhaoxi/i18n";const L:any={verified_partner:"Verified Partner",high_service_quality:"High Service Quality",reliable_support:"Reliable Support",quality_recovered:"Quality Recovered"};const C={"zh-CN":{verified:"平台人工认证",view:"查看认证档案"},"zh-TW":{verified:"平台人工認證",view:"查看認證檔案"},"vi-VN":{verified:"Được nền tảng xác minh thủ công",view:"Xem hồ sơ xác minh"},"en-US":{verified:"Manually verified by the platform",view:"View verification profile"}}as const;export default function VerifiedPartnerIdentity({organizationId,compact=false}:{organizationId:string;compact?:boolean}){const{locale}=useZhaoXiLocale(),t=C[locale];const[d,setD]=useState<any>(null);useEffect(()=>{if(!organizationId)return;fetch(`/api/public/partners/${organizationId}/trust`,{cache:"no-store"}).then(r=>r.json()).then(j=>j?.ok&&setD(j.data?.partner))},[organizationId]);if(!d?.badges?.length)return null;const content=<><div style={{display:"flex",gap:4,flexWrap:"wrap"}}>{d.badges.map((x:any)=><span key={x.type} title={t.verified} style={{fontSize:compact?7:8,fontWeight:900,color:"#067647"}}>✓ {L[x.type]||x.label}</span>)}</div>{!compact&&<span style={{fontSize:8,color:"#078343",fontWeight:800}}>{t.view} ›</span>}</>;return <Link href={`/partners/${organizationId}`} style={{display:"block",padding:compact?5:8,border:"1px solid #ccebd8",borderRadius:compact?9:12,background:"#f4fff8",margin:"5px 0",textDecoration:"none",color:"inherit"}}>{content}</Link>}
