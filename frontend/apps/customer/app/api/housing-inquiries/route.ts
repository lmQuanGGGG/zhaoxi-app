import {NextRequest} from "next/server";export const dynamic="force-dynamic";const backend=()=>((process.env.ZHAOXI_BACKEND_URL||process.env.NEXT_PUBLIC_ZHAOXI_API_URL||"").includes("zhaoxi-backend.vercel.app")?"https://zhaoxi-app-puce.vercel.app":(process.env.ZHAOXI_BACKEND_URL||process.env.NEXT_PUBLIC_ZHAOXI_API_URL||"https://zhaoxi-app-puce.vercel.app")).replace(/\/+$/,"");
export async function POST(r:NextRequest){const token=r.cookies.get("zx_access_v2")?.value;const headers:Record<string,string>={"content-type":"application/json"};if(token)headers.authorization=`Bearer ${token}`;try{const incoming=await r.json();const {customerPhone:browserCustomerPhone,...payload}=incoming||{};void browserCustomerPhone;const u=await fetch(`${backend()}/api/housing-inquiries`,{method:"POST",headers,body:JSON.stringify(payload),cache:"no-store"});return Response.json(await u.json(),{status:u.status})}catch{return Response.json({ok:false,error:{code:"HOUSING_INQUIRY_UNAVAILABLE"}},{status:503})}}
