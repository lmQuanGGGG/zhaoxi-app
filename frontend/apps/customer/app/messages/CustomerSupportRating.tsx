"use client";import{useEffect,useState}from"react";import{useZhaoXiLocale}from"@zhaoxi/i18n";
const C={"zh-CN":{title:"这次支持体验如何？",send:"提交评价",placeholder:"可选留言",thanks:"感谢您的反馈"},"zh-TW":{title:"這次支援體驗如何？",send:"提交評價",placeholder:"可選留言",thanks:"感謝您的回饋"},"vi-VN":{title:"Bạn đánh giá hỗ trợ lần này thế nào?",send:"Gửi đánh giá",placeholder:"Ghi chú thêm (không bắt buộc)",thanks:"Cảm ơn phản hồi của bạn"},"en-US":{title:"How was this support experience?",send:"Submit rating",placeholder:"Optional comment",thanks:"Thanks for your feedback"}}as const;
export default function CustomerSupportRating({threadId,status}:{threadId:string;status:string}){const{locale}=useZhaoXiLocale(),t=C[locale];const[rating,setRating]=useState(0),[comment,setComment]=useState(""),[done,setDone]=useState(false);useEffect(()=>{if(status!=="resolved")return;fetch(`/api/customer-support-satisfaction/${threadId}`,{cache:"no-store"}).then(r=>r.json()).then(j=>{const x=j?.data?.satisfaction;if(x){setRating(x.rating);setComment(x.comment||"");setDone(true)}}).catch(()=>{})},[threadId,status]);if(status!=="resolved")return null;async function save(){if(rating<1)return;const r=await fetch(`/api/customer-support-satisfaction/${threadId}`,{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({rating,comment})});if(r.ok)setDone(true)}return <section style={{padding:10,border:"1px solid #dfe8e3",borderRadius:13,background:"#fff7ed",marginBottom:10}}><b>⭐ {done?t.thanks:t.title}</b><div style={{display:"flex",gap:5,margin:"7px 0"}}>{[1,2,3,4,5].map(n=><button key={n} onClick={()=>{setRating(n);setDone(false)}} style={{border:0,background:"transparent",fontSize:22,opacity:n<=rating?1:.25}}>★</button>)}</div><textarea value={comment} onChange={e=>{setComment(e.target.value);setDone(false)}} placeholder={t.placeholder} rows={2} style={{width:"100%",boxSizing:"border-box"}}/><button onClick={()=>void save()} disabled={!rating} style={{marginTop:6}}>{t.send}</button></section>}
