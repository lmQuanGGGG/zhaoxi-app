# Sprint 14.5.1

Hotfix: align `@zhaoxi/payment` dependency versions in Customer, Partner and Admin with the local workspace package version `14.5.0`. This prevents npm from attempting to download the private workspace package from the public npm registry.
