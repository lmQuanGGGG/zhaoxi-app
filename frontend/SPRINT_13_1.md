# Sprint 13.1 Marketplace Core

- Media metadata is registered in backend `media_assets`.
- Blob remains file storage; database is the source of truth.
- Partner publish endpoint promotes logo, banners, product images and services.
- Customer/Admin continue reading published organization and service metadata.
- Run backend migration `npm run db:migrate` before deployment.
