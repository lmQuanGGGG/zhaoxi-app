# Sprint 12.2.2 — End-to-end service requests

Customer flow: Service detail → request form → backend → Neon → success → orders → request history.

Security note: customer order listing is scoped by locally-held request codes or exact phone, never an unfiltered public list.
