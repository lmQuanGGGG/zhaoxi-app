# ZhaoXi 17.7.5 — Exact Mobile UI Match

Mobile Admin overview follows the approved reference exactly in information hierarchy:
Top bar → welcome/range → 4 KPI cards → Top Partners → Service Overview → 5-item bottom nav.

The overview notification panel is removed; notification access remains in the shared top-bar bell.
The previous horizontal module rail is removed from the overview. The hamburger opens the functional Admin module drawer.
Business/API logic is unchanged.
