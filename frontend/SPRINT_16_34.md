# Sprint 16.34 — Restaurant Finance Ledger & Settlement Detail

Admin settlement adds order ledger, reasoned adjustments, payment/status history and CSV export. Partner receives read-only settlement drill-down and CSV export. Single-language UI remains mandatory.
