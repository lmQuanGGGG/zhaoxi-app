import fs from "node:fs";import path from "node:path";const root=process.cwd();const must=["lib/services/health-service.ts","app/api/health/route.ts","app/api/readiness/route.ts","app/api/version/route.ts","SPRINT_15_0.md"];for(const file of must){if(!fs.existsSync(path.join(root,file))) throw new Error(`Missing ${file}`)}const pkg=JSON.parse(fs.readFileSync(path.join(root,"package.json"),"utf8"));if(pkg.version!=="15.0.0") throw new Error("Backend version must be 15.0.0");if(!pkg.scripts?.["verify:15.0"]) throw new Error("Missing verify:15.0 script");console.log("Sprint 15.0 Backend Beta Release Hardening structure is valid.");
