import dotenv from "dotenv";import postgres from "postgres";dotenv.config({path:".env.local"});dotenv.config();const url=process.env.DATABASE_URL_UNPOOLED||process.env.POSTGRES_URL_NON_POOLING||process.env.POSTGRES_URL||process.env.DATABASE_URL;if(!url)throw new Error("DATABASE_URL required");const sql=postgres(url,{ssl:"require",max:1,prepare:false});try{await sql`create table if not exists qr_pairing_sessions(id uuid primary key default gen_random_uuid(),secret_hash varchar(64) not null,requested_role varchar(24) not null,locale varchar(10) not null default 'zh-CN',status varchar(24) not null default 'waiting_scan',user_id uuid references users(id) on delete set null,exchange_code_hash varchar(64),exchange_expires_at timestamptz,exchanged_at timestamptz,expires_at timestamptz not null,confirmed_at timestamptz,created_at timestamptz not null default now(),updated_at timestamptz not null default now())`;await sql`create index if not exists qr_pairing_sessions_status_idx on qr_pairing_sessions(status)`;await sql`create index if not exists qr_pairing_sessions_expires_idx on qr_pairing_sessions(expires_at)`;console.log("Sprint 16.12.1 secure QR pairing migration applied.")}finally{await sql.end({timeout:5})}