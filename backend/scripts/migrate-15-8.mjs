import dotenv from "dotenv";import postgres from "postgres";dotenv.config({path:".env.local"});dotenv.config();const url=process.env.DATABASE_URL_UNPOOLED||process.env.POSTGRES_URL_NON_POOLING||process.env.POSTGRES_URL||process.env.DATABASE_URL;if(!url)throw new Error("POSTGRES_URL or DATABASE_URL is required");const sql=postgres(url,{ssl:"require",max:1,prepare:false});try{await sql.unsafe(`CREATE TABLE IF NOT EXISTS feature_flags (id uuid PRIMARY KEY DEFAULT gen_random_uuid(),key varchar(120) NOT NULL UNIQUE,name varchar(180) NOT NULL,description text,enabled boolean NOT NULL DEFAULT false,kill_switch boolean NOT NULL DEFAULT false,channels jsonb NOT NULL DEFAULT '["beta"]'::jsonb,roles jsonb NOT NULL DEFAULT '["customer","partner","driver","admin"]'::jsonb,rollout_percent integer NOT NULL DEFAULT 100,metadata jsonb NOT NULL DEFAULT '{}'::jsonb,created_by uuid REFERENCES users(id) ON DELETE SET NULL,updated_by uuid REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS feature_flags_enabled_idx ON feature_flags(enabled);CREATE INDEX IF NOT EXISTS feature_flags_key_idx ON feature_flags(key);`);await sql.unsafe(`INSERT INTO feature_flags (key,name,description,enabled,channels,roles,rollout_percent) VALUES ('marketplace.search2','Marketplace Search 2.0','Dynamic marketplace search and recommendations',true,'["beta","canary","stable"]','["customer"]',100),('delivery.live-gps','Live GPS Delivery','Realtime delivery tracking',true,'["beta","canary"]','["customer","partner","driver","admin"]',100),('payment.wechat','WeChat Pay','WeChat Pay checkout availability',false,'["beta","canary"]','["customer","partner","admin"]',100),('support.ai','AI Support','AI customer service integration',true,'["beta","canary"]','["customer","partner","driver","admin"]',100) ON CONFLICT (key) DO NOTHING;`);console.log("Sprint 15.8 feature flags migration applied.")}finally{await sql.end({timeout:5})}