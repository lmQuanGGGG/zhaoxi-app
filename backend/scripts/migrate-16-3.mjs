import dotenv from "dotenv";import postgres from "postgres";dotenv.config({path:".env.local"});dotenv.config();const url=process.env.DATABASE_URL_UNPOOLED||process.env.POSTGRES_URL_NON_POOLING||process.env.POSTGRES_URL||process.env.DATABASE_URL;if(!url)throw new Error("POSTGRES_URL or DATABASE_URL is required");const sql=postgres(url,{ssl:"require",max:1,prepare:false});try{await sql.unsafe(`CREATE TABLE IF NOT EXISTS release_alert_policies (id uuid PRIMARY KEY DEFAULT gen_random_uuid(),key varchar(80) NOT NULL UNIQUE,name varchar(180) NOT NULL,metric varchar(80) NOT NULL,comparator varchar(8) NOT NULL DEFAULT '>=',threshold numeric(12,2) NOT NULL,severity varchar(16) NOT NULL DEFAULT 'warning',window_minutes integer NOT NULL DEFAULT 60,enabled boolean NOT NULL DEFAULT true,cooldown_minutes integer NOT NULL DEFAULT 30,created_by uuid REFERENCES users(id) ON DELETE SET NULL,updated_by uuid REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS release_alert_policies_enabled_idx ON release_alert_policies(enabled);CREATE TABLE IF NOT EXISTS release_incidents (id uuid PRIMARY KEY DEFAULT gen_random_uuid(),policy_id uuid REFERENCES release_alert_policies(id) ON DELETE SET NULL,policy_key varchar(80) NOT NULL,metric varchar(80) NOT NULL,severity varchar(16) NOT NULL,status varchar(24) NOT NULL DEFAULT 'open',observed_value numeric(12,2) NOT NULL,threshold numeric(12,2) NOT NULL,window_minutes integer NOT NULL,message text NOT NULL,release_version varchar(40),acknowledged_by uuid REFERENCES users(id) ON DELETE SET NULL,acknowledged_at timestamptz,resolved_by uuid REFERENCES users(id) ON DELETE SET NULL,resolved_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS release_incidents_status_idx ON release_incidents(status);CREATE INDEX IF NOT EXISTS release_incidents_created_idx ON release_incidents(created_at);`);await sql.unsafe(`INSERT INTO release_alert_policies(key,name,metric,comparator,threshold,severity,window_minutes,enabled,cooldown_minutes) VALUES ('runtime-critical','Critical runtime events','criticalErrors','>=',1,'critical',60,true,30),('runtime-error-spike','Runtime error spike','runtimeErrors','>=',10,'warning',60,true,30),('order-failure-rate','Order failure rate','orderFailureRate','>=',20,'warning',60,true,30),('payment-failure-rate','Payment failure rate','paymentFailureRate','>=',15,'warning',60,true,30),('support-backlog','Support backlog','openSupport','>=',25,'warning',60,true,60) ON CONFLICT(key) DO NOTHING;`);console.log("Sprint 16.3 alert policy migration applied.")}finally{await sql.end({timeout:5})}