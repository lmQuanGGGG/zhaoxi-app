import dotenv from "dotenv";import postgres from "postgres";dotenv.config({path:".env.local"});dotenv.config();const url=process.env.DATABASE_URL_UNPOOLED||process.env.POSTGRES_URL_NON_POOLING||process.env.POSTGRES_URL||process.env.DATABASE_URL;if(!url)throw new Error("POSTGRES_URL or DATABASE_URL is required");const sql=postgres(url,{ssl:"require",max:1,prepare:false});try{await sql.unsafe(`CREATE TABLE IF NOT EXISTS onboarding_applications (id uuid PRIMARY KEY DEFAULT gen_random_uuid(),user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,role varchar(24) NOT NULL,status varchar(24) NOT NULL DEFAULT 'draft',display_name varchar(120),phone varchar(30),city varchar(120),business_name varchar(180),business_type varchar(40),address_text text,vehicle_type varchar(40),plate_number varchar(32),notes text,review_note text,reviewed_by uuid REFERENCES users(id) ON DELETE SET NULL,submitted_at timestamptz,reviewed_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(user_id,role));CREATE INDEX IF NOT EXISTS onboarding_status_idx ON onboarding_applications(status);CREATE INDEX IF NOT EXISTS onboarding_role_idx ON onboarding_applications(role);`);console.log("Sprint 15.4 onboarding migration applied.")}finally{await sql.end({timeout:5})}
