import dotenv from "dotenv";import postgres from "postgres";dotenv.config({path:".env.local"});dotenv.config();const url=process.env.DATABASE_URL_UNPOOLED||process.env.POSTGRES_URL_NON_POOLING||process.env.POSTGRES_URL||process.env.DATABASE_URL;if(!url)throw new Error("POSTGRES_URL or DATABASE_URL is required");const sql=postgres(url,{ssl:"require",max:1,prepare:false});try{await sql.unsafe(`CREATE TABLE IF NOT EXISTS beta_invites (id uuid PRIMARY KEY DEFAULT gen_random_uuid(),code_hash varchar(64) NOT NULL UNIQUE,code_hint varchar(16) NOT NULL,label varchar(120),role varchar(24) NOT NULL DEFAULT 'customer',max_uses integer NOT NULL DEFAULT 1,used_count integer NOT NULL DEFAULT 0,status varchar(24) NOT NULL DEFAULT 'active',expires_at timestamptz,created_by uuid REFERENCES users(id) ON DELETE SET NULL,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS beta_invites_status_idx ON beta_invites(status);CREATE INDEX IF NOT EXISTS beta_invites_role_idx ON beta_invites(role);CREATE TABLE IF NOT EXISTS beta_access (id uuid PRIMARY KEY DEFAULT gen_random_uuid(),user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,role varchar(24) NOT NULL,status varchar(24) NOT NULL DEFAULT 'active',source varchar(24) NOT NULL DEFAULT 'invite',invite_id uuid REFERENCES beta_invites(id) ON DELETE SET NULL,granted_by uuid REFERENCES users(id) ON DELETE SET NULL,notes text,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now(),UNIQUE(user_id,role));CREATE INDEX IF NOT EXISTS beta_access_status_idx ON beta_access(status);CREATE TABLE IF NOT EXISTS beta_invite_redemptions (id uuid PRIMARY KEY DEFAULT gen_random_uuid(),invite_id uuid NOT NULL REFERENCES beta_invites(id) ON DELETE CASCADE,user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,role varchar(24) NOT NULL,created_at timestamptz NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS beta_redemptions_invite_idx ON beta_invite_redemptions(invite_id);CREATE INDEX IF NOT EXISTS beta_redemptions_user_idx ON beta_invite_redemptions(user_id);`);console.log("Sprint 15.3 beta access migration applied.")}finally{await sql.end({timeout:5})}
