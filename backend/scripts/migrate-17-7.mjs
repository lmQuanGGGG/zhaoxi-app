import postgres from"postgres";import dotenv from"dotenv";dotenv.config({path:".env.local"});dotenv.config();const url=process.env.DATABASE_URL||process.env.POSTGRES_URL;if(!url)throw new Error("DATABASE_URL/POSTGRES_URL missing");const sql=postgres(url,{ssl:"require"});try{
await sql`create table if not exists operations_playbooks(id uuid primary key default gen_random_uuid(),code varchar(80) not null unique,name varchar(180) not null,work_kind varchar(48) not null default 'all',description text,trigger_risk varchar(24) not null default 'critical',steps jsonb not null default '[]'::jsonb,is_enabled boolean not null default true,created_by_admin_user_id uuid references users(id) on delete set null,updated_at timestamptz not null default now())`;
await sql`create table if not exists operations_workflow_runs(id uuid primary key default gen_random_uuid(),work_kind varchar(48) not null,work_id varchar(120) not null,playbook_id uuid not null references operations_playbooks(id) on delete restrict,status varchar(24) not null default 'proposed',current_step integer not null default 0,context jsonb not null default '{}'::jsonb,proposed_by_admin_user_id uuid references users(id) on delete set null,approved_by_admin_user_id uuid references users(id) on delete set null,approved_at timestamptz,completed_at timestamptz,created_at timestamptz not null default now(),updated_at timestamptz not null default now())`;
await sql`create index if not exists operations_workflow_runs_work_idx on operations_workflow_runs(work_kind,work_id)`;await sql`create index if not exists operations_workflow_runs_status_idx on operations_workflow_runs(status,created_at)`;
await sql`create table if not exists operations_approval_requests(id uuid primary key default gen_random_uuid(),workflow_run_id uuid not null references operations_workflow_runs(id) on delete cascade,step_key varchar(80) not null,title varchar(220) not null,reason text,status varchar(24) not null default 'pending',requested_by_admin_user_id uuid references users(id) on delete set null,decided_by_admin_user_id uuid references users(id) on delete set null,decision_note text,decided_at timestamptz,created_at timestamptz not null default now())`;await sql`create index if not exists operations_approval_requests_status_idx on operations_approval_requests(status,created_at)`;
await sql`create table if not exists operations_workflow_events(id uuid primary key default gen_random_uuid(),workflow_run_id uuid not null references operations_workflow_runs(id) on delete cascade,actor_admin_user_id uuid references users(id) on delete set null,event_type varchar(60) not null,metadata jsonb not null default '{}'::jsonb,created_at timestamptz not null default now())`;await sql`create index if not exists operations_workflow_events_run_idx on operations_workflow_events(workflow_run_id,created_at)`;
await sql`insert into operations_playbooks(code,name,work_kind,description,trigger_risk,steps) values ('sla_escalation','SLA Escalation','all','Review SLA risk, assign owner and escalate with human approval.','critical','[{"key":"review","title":"Review context"},{"key":"assign","title":"Confirm owner","requiresApproval":true},{"key":"escalate","title":"Approve escalation","requiresApproval":true},{"key":"followup","title":"Schedule follow-up"}]'::jsonb),('critical_recovery','Critical Service Recovery','service_recovery','Structured recovery handling with explicit approval gates.','critical','[{"key":"assess","title":"Assess impact"},{"key":"recovery_plan","title":"Approve recovery plan","requiresApproval":true},{"key":"execute","title":"Execute approved operational actions"},{"key":"verify","title":"Verify recovery"}]'::jsonb),('support_overdue','Overdue Support','support','Recover overdue support case with supervisor approval.','overdue','[{"key":"review","title":"Review conversation"},{"key":"supervisor","title":"Supervisor escalation","requiresApproval":true},{"key":"response","title":"Prepare response"},{"key":"verify","title":"Verify SLA recovery"}]'::jsonb) on conflict(code) do nothing`;
console.log("ZhaoXi 17.7 Operations Workflow Orchestration, Escalation Playbooks & Approval Center migration applied.");}finally{await sql.end()}
