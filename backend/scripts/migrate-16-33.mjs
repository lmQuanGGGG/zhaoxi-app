import dotenv from "dotenv";import postgres from "postgres";dotenv.config({path:".env.local"});dotenv.config();
const url=process.env.DATABASE_URL_UNPOOLED||process.env.POSTGRES_URL_NON_POOLING||process.env.POSTGRES_URL||process.env.DATABASE_URL;if(!url)throw new Error("DATABASE_URL required");const sql=postgres(url,{ssl:"require",max:1,prepare:false});
try{await sql`create table if not exists restaurant_settlements(id uuid primary key default gen_random_uuid(),organization_id uuid not null references organizations(id) on delete cascade,period_start timestamptz not null,period_end timestamptz not null,status varchar(24) not null default 'draft',currency varchar(8) not null default 'VND',order_count integer not null default 0,item_base_revenue integer not null default 0,promotion_discount integer not null default 0,coupon_discount integer not null default 0,food_revenue integer not null default 0,delivery_gross_fee integer not null default 0,delivery_subsidy integer not null default 0,customer_delivery_fee integer not null default 0,customer_paid integer not null default 0,partner_payable integer not null default 0,platform_commission integer not null default 0,adjustments integer not null default 0,snapshot jsonb not null default '{}'::jsonb,created_by_user_id uuid references users(id) on delete set null,confirmed_by_user_id uuid references users(id) on delete set null,paid_by_user_id uuid references users(id) on delete set null,confirmed_at timestamptz,paid_at timestamptz,created_at timestamptz not null default now(),updated_at timestamptz not null default now(),unique(organization_id,period_start,period_end))`;await sql`create index if not exists restaurant_settlements_org_idx on restaurant_settlements(organization_id)`;await sql`create index if not exists restaurant_settlements_status_idx on restaurant_settlements(status)`;console.log("Sprint 16.33 restaurant financial reconciliation & settlement migration applied.")}finally{await sql.end({timeout:5})}
