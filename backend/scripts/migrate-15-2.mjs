import dotenv from "dotenv";import postgres from "postgres";dotenv.config({path:".env.local"});dotenv.config();const url=process.env.DATABASE_URL_UNPOOLED||process.env.POSTGRES_URL_NON_POOLING||process.env.POSTGRES_URL||process.env.DATABASE_URL;if(!url)throw new Error("POSTGRES_URL or DATABASE_URL is required");const sql=postgres(url,{ssl:"require",max:1,prepare:false});try{await sql.unsafe(`CREATE TABLE IF NOT EXISTS beta_feedback (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), user_id uuid REFERENCES users(id) ON DELETE SET NULL, organization_id uuid REFERENCES organizations(id) ON DELETE SET NULL, app varchar(24) NOT NULL, category varchar(32) NOT NULL DEFAULT 'general', rating integer, message text NOT NULL, route text, release varchar(40) NOT NULL DEFAULT '15.2.0', channel varchar(16) NOT NULL DEFAULT 'beta', status varchar(24) NOT NULL DEFAULT 'new', metadata jsonb NOT NULL DEFAULT '{}'::jsonb, created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now());`);await sql.unsafe(`CREATE INDEX IF NOT EXISTS beta_feedback_created_idx ON beta_feedback(created_at);CREATE INDEX IF NOT EXISTS beta_feedback_app_idx ON beta_feedback(app);CREATE INDEX IF NOT EXISTS beta_feedback_status_idx ON beta_feedback(status);CREATE INDEX IF NOT EXISTS beta_feedback_release_idx ON beta_feedback(release);`);console.log("Sprint 15.2 beta feedback migration applied.")}finally{await sql.end({timeout:5})}
