import "dotenv/config";import postgres from "postgres";const connection=process.env.DATABASE_URL_UNPOOLED||process.env.POSTGRES_URL_NON_POOLING||process.env.DATABASE_URL||process.env.POSTGRES_URL;if(!connection)throw new Error("DATABASE_URL or POSTGRES_URL is required");const sql=postgres(connection,{max:1,prepare:false});try{await sql.unsafe(`CREATE TABLE IF NOT EXISTS support_conversations(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),user_id uuid REFERENCES users(id) ON DELETE SET NULL,organization_id uuid REFERENCES organizations(id) ON DELETE SET NULL,role varchar(24) NOT NULL DEFAULT 'customer',locale varchar(10) NOT NULL DEFAULT 'vi-VN',subject varchar(240) NOT NULL DEFAULT 'ZhaoXi Support',status varchar(24) NOT NULL DEFAULT 'open',metadata jsonb NOT NULL DEFAULT '{}'::jsonb,last_message_at timestamptz NOT NULL DEFAULT now(),created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS support_conversations_user_idx ON support_conversations(user_id);CREATE INDEX IF NOT EXISTS support_conversations_org_idx ON support_conversations(organization_id);CREATE INDEX IF NOT EXISTS support_conversations_status_idx ON support_conversations(status);CREATE TABLE IF NOT EXISTS support_messages(id uuid PRIMARY KEY DEFAULT gen_random_uuid(),conversation_id uuid NOT NULL REFERENCES support_conversations(id) ON DELETE CASCADE,sender_role varchar(24) NOT NULL,body text NOT NULL,intent varchar(60),metadata jsonb NOT NULL DEFAULT '{}'::jsonb,created_at timestamptz NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS support_messages_conversation_idx ON support_messages(conversation_id);CREATE INDEX IF NOT EXISTS support_messages_created_idx ON support_messages(created_at);`);console.log("Sprint 14.7 support migration applied.");}finally{await sql.end({timeout:5});}\n