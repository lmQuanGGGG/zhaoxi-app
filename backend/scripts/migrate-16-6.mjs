import dotenv from "dotenv";import postgres from "postgres";dotenv.config({path:".env.local"});dotenv.config();const url=process.env.DATABASE_URL_UNPOOLED||process.env.POSTGRES_URL_NON_POOLING||process.env.POSTGRES_URL||process.env.DATABASE_URL;if(!url)throw new Error("POSTGRES_URL or DATABASE_URL is required");const sql=postgres(url,{ssl:"require",max:1,prepare:false});try{await sql.unsafe(`ALTER TABLE runtime_controls ADD COLUMN IF NOT EXISTS public_rollout_percent integer NOT NULL DEFAULT 100;UPDATE runtime_controls SET public_rollout_percent=100 WHERE public_rollout_percent IS NULL;`);console.log("Sprint 16.6 staged public rollout migration applied.")}finally{await sql.end({timeout:5})}