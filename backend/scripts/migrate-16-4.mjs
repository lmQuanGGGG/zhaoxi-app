import dotenv from "dotenv";import postgres from "postgres";dotenv.config({path:".env.local"});dotenv.config();const url=process.env.DATABASE_URL_UNPOOLED||process.env.POSTGRES_URL_NON_POOLING||process.env.POSTGRES_URL||process.env.DATABASE_URL;if(!url)throw new Error("POSTGRES_URL or DATABASE_URL is required");const sql=postgres(url,{ssl:"require",max:1,prepare:false});try{await sql.unsafe(`CREATE TABLE IF NOT EXISTS ui_acceptance_items (id uuid PRIMARY KEY DEFAULT gen_random_uuid(),item_key varchar(120) NOT NULL UNIQUE,app varchar(24) NOT NULL,category varchar(60) NOT NULL,title varchar(220) NOT NULL,route varchar(240),priority varchar(16) NOT NULL DEFAULT 'high',status varchar(24) NOT NULL DEFAULT 'pending',notes text,evidence_url text,updated_by uuid REFERENCES users(id) ON DELETE SET NULL,reviewed_at timestamptz,created_at timestamptz NOT NULL DEFAULT now(),updated_at timestamptz NOT NULL DEFAULT now());CREATE INDEX IF NOT EXISTS ui_acceptance_items_app_idx ON ui_acceptance_items(app);CREATE INDEX IF NOT EXISTS ui_acceptance_items_status_idx ON ui_acceptance_items(status);CREATE INDEX IF NOT EXISTS ui_acceptance_items_priority_idx ON ui_acceptance_items(priority);`);await sql.unsafe(`INSERT INTO ui_acceptance_items(item_key,app,category,title,route,priority,status) VALUES ('auth-wechat-all','all','Authentication','WeChat QR login appears and routes correctly on Customer, Partner and Admin','/','critical','pending'),('mobile-topbar','all','Mobile UI','Language, theme and logout controls align inside mobile top bar','/','critical','pending'),('locale-single-language','all','Localization','Dialogs, search boxes and redirects show only the selected language','/','critical','pending'),('customer-home','customer','Customer','Customer home uses current mobile layout and current service routes','/','critical','pending'),('customer-food','customer','Ordering','Food flow stays in current restaurant UI without legacy redirect','/food','critical','pending'),('customer-banner','customer','Visual','Restaurant banner runs as fade slideshow when multiple images exist','/food','high','pending'),('customer-cart-zero','customer','Ordering','Menu quantity defaults to 0 and selected quantity is preserved in checkout','/food','critical','pending'),('customer-cart-org','customer','Ordering','Cart remains separated per restaurant / organization','/cart','critical','pending'),('customer-notifications','customer','Notifications','Order-stage notification appears once, supports unread and notification center','/messages','high','pending'),('partner-store','partner','Partner','Store management and order management tabs remain available and mobile-sized','/','critical','pending'),('partner-order-alert','partner','Orders','New order popup works from either tab and requests ETA','/','critical','pending'),('partner-eta','partner','Orders','Accepted ETA flows to Customer and completion transition is correct','/','high','pending'),('partner-catalog-media','partner','Catalog','Product image synchronizes to matching menu item without second upload','/catalog','high','pending'),('driver-jobs','driver','Driver','Driver can see assigned jobs and current delivery state','/','critical','pending'),('driver-gps','driver','Tracking','Realtime GPS tracking renders and updates without layout overflow','/','critical','pending'),('admin-orders','admin','Admin','Orders are grouped by service area instead of one long list','/','high','pending'),('admin-release','admin','Release','Release Center, health, rollback and alerts fit mobile viewport','/release-center','critical','pending'),('account-center','all','Account','Account Center shows roles, profile, devices and safe role switching','/account','critical','pending'),('payment-flow','customer','Payment','Payment method and payment status remain visible through order detail','/payment','critical','pending'),('mobile-no-overflow','all','Mobile UI','No horizontal overflow at 390-430 px viewport on primary screens','/','critical','pending') ON CONFLICT(item_key) DO NOTHING;`);console.log("Sprint 16.4 UI acceptance checklist migration applied.")}finally{await sql.end({timeout:5})}