import dotenv from "dotenv";import postgres from "postgres";dotenv.config({path:".env.local"});dotenv.config();
const url=process.env.DATABASE_URL_UNPOOLED||process.env.POSTGRES_URL_NON_POOLING||process.env.POSTGRES_URL||process.env.DATABASE_URL;if(!url)throw new Error("DATABASE_URL required");const sql=postgres(url,{ssl:"require",max:1,prepare:false});
try{await sql`create table if not exists restaurant_settlement_adjustments(id uuid primary key default gen_random_uuid(),settlement_id uuid not null references restaurant_settlements(id) on delete cascade,amount integer not null,reason varchar(500) not null,created_by_user_id uuid references users(id) on delete set null,created_at timestamptz not null default now())`;await sql`create index if not exists restaurant_settlement_adjustments_settlement_idx on restaurant_settlement_adjustments(settlement_id)`;console.log("Sprint 16.34 restaurant finance ledger & settlement detail migration applied.")}finally{await sql.end({timeout:5})}
