# Sprint 12.5.1.1 integrated correction

The API routing remains direct: services.organization_id -> service_requests.assigned_organization_id.
The backend root page now identifies the deployment as API infrastructure and links to the three applications.
