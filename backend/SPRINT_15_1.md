# Sprint 15.1 — Beta Observability & Incident Center

Adds centralized runtime event capture, incident summaries and an Admin incident center. Sensitive fields such as tokens, secrets, passwords, authorization and cookies are stripped from client metadata before persistence.

## Commands
`npm run db:apply:15.1` → `npm run verify:15.1` → `npm run typecheck` → `npm run build`.
