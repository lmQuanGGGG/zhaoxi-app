# Sprint 14.9 — Operations Analytics & Platform Health

Adds `/api/analytics/overview` and `AnalyticsService`. Supports platform-wide or organization-scoped metrics over 7/30/90+ day windows without schema changes.
