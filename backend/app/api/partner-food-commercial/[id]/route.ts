import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {foodCommercialService} from "@/lib/services/food-commercial-service";export const dynamic="force-dynamic";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const s=await authenticatedSession(request);if(!s||s.role!=="partner")return failure("Partner required.",403,undefined,"PARTNER_REQUIRED");try{const{id}=await params;return success(await foodCommercialService.update(s.userId,id,await request.json().catch(()=>({}))))}catch(e){const code=e instanceof Error?e.message:"FOOD_COMMERCIAL_UPDATE_FAILED";return failure("Unable to update food commercial settings.",code==="PARTNER_FORBIDDEN"?403:422,undefined,code)}}
