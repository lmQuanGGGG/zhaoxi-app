import {failure,success} from "@/lib/core/api-response";import {authenticatedSession} from "@/lib/auth-request";import {onboardingService} from "@/lib/services/onboarding-service";export async function POST(r:Request){try{const s=await authenticatedSession(r);if(!s)return failure("Authentication required.",401);const b=await r.json();return success(await onboardingService.submit(s.userId,b),{status:201})}catch(e){return failure(e instanceof Error?e.message:"Unable to submit onboarding.",422)}}
