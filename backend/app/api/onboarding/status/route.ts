import {failure,success} from "@/lib/core/api-response";import {authenticatedSession} from "@/lib/auth-request";import {onboardingService} from "@/lib/services/onboarding-service";export const dynamic="force-dynamic";export async function GET(r:Request){const s=await authenticatedSession(r);return s?success(await onboardingService.status(s.userId)):failure("Authentication required.",401)}
