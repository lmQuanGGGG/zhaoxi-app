import {failure,success} from "@/lib/core/api-response";import {authenticatedSession} from "@/lib/auth-request";import {onboardingService} from "@/lib/services/onboarding-service";export async function PATCH(r:Request,{params}:{params:Promise<{id:string}>}){try{const s=await authenticatedSession(r);if(s?.role!=="admin")return failure("Admin access required.",403);const{id}=await params;return success(await onboardingService.review(id,await r.json(),s.userId))}catch(e){return failure(e instanceof Error?e.message:"Unable to review onboarding.",422)}}
