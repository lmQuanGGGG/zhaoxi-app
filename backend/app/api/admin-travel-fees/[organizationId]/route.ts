import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {travelPlatformFeeService} from "@/lib/services/travel-platform-fee-service";export const dynamic="force-dynamic";
export async function PATCH(request:Request,{params}:{params:Promise<{organizationId:string}>}){const s=await authenticatedSession(request);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");try{const{organizationId}=await params;return success(await travelPlatformFeeService.update(s.userId,organizationId,await request.json()))}catch(e){const code=e instanceof Error?e.message:"TRAVEL_PLATFORM_FEE_UPDATE_FAILED";return failure("Unable to update Travel platform fee policy.",code==="ORGANIZATION_NOT_FOUND"?404:422,undefined,code)}}