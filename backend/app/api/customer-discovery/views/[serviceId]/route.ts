import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{customerPersonalizedDiscoveryService as s}from"@/lib/services/customer-personalized-discovery-service";export async function POST(r:Request,{params}:{params:Promise<{serviceId:string}>}){const a=await authenticatedSession(r);if(!a||a.role!=="customer")return failure("Customer required.",401,undefined,"CUSTOMER_REQUIRED");const{serviceId}=await params;return success(await s.recordView(a.userId,serviceId))}