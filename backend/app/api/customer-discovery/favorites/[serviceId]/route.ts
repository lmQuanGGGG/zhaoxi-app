import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{customerPersonalizedDiscoveryService as s}from"@/lib/services/customer-personalized-discovery-service";export async function GET(r:Request,{params}:{params:Promise<{serviceId:string}>}){const a=await authenticatedSession(r);if(!a||a.role!=="customer")return failure("Customer required.",401,undefined,"CUSTOMER_REQUIRED");const{serviceId}=await params;return success(await s.isFavorite(a.userId,serviceId))}export async function PUT(r:Request,{params}:{params:Promise<{serviceId:string}>}){const a=await authenticatedSession(r);if(!a||a.role!=="customer")return failure("Customer required.",401,undefined,"CUSTOMER_REQUIRED");try{const{serviceId}=await params,b=await r.json().catch(()=>({}));return success(await s.toggleFavorite(a.userId,serviceId,typeof b.favorite==="boolean"?b.favorite:undefined))}catch(e){return failure("Unable to update favorite.",422,undefined,e instanceof Error?e.message:"FAVORITE_FAILED")}}