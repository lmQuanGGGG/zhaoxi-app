import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {travelReminderService} from "@/lib/services/travel-reminder-service";export const dynamic="force-dynamic";export async function POST(request:Request){const s=await authenticatedSession(request);if(!s||!["admin","partner"].includes(s.role))return failure("Authorized role required.",403,undefined,"ROLE_REQUIRED");try{return success(await travelReminderService.evaluate())}catch(e){console.error(e);return failure("Unable to evaluate Travel reminders.",500,undefined,"TRAVEL_REMINDER_EVALUATE_FAILED")}}