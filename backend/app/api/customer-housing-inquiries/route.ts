import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {housingLeadService} from "@/lib/services/housing-lead-service";export const dynamic="force-dynamic";
export async function GET(request:Request){const s=await authenticatedSession(request);if(!s||s.role!=="customer")return failure("Customer required.",401,undefined,"CUSTOMER_REQUIRED");try{return success(await housingLeadService.listCustomer(s.userId))}catch(e){console.error(e);return failure("Unable to load housing inquiries.",500,undefined,"CUSTOMER_HOUSING_INQUIRIES_FAILED")}}
