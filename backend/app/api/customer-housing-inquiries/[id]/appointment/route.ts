import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {housingAppointmentService} from "@/lib/services/housing-appointment-service";export const dynamic="force-dynamic";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const s=await authenticatedSession(request);if(!s||s.role!=="customer")return failure("Customer required.",401,undefined,"CUSTOMER_REQUIRED");try{const{id}=await params;return success(await housingAppointmentService.customerAction(s.userId,id,await request.json().catch(()=>({}))))}catch(e){const code=e instanceof Error?e.message:"HOUSING_APPOINTMENT_UPDATE_FAILED";const status=code.includes("FORBIDDEN")?403:422;return failure("Unable to update housing appointment.",status,undefined,code)}}
