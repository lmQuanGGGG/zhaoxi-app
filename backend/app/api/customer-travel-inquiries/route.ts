import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {travelBookingService} from "@/lib/services/travel-booking-service";export const dynamic="force-dynamic";
export async function GET(request:Request){const s=await authenticatedSession(request);if(!s||s.role!=="customer")return failure("Customer required.",401,undefined,"CUSTOMER_REQUIRED");try{return success(await travelBookingService.customerList(s.userId))}catch(e){console.error(e);return failure("Unable to load travel inquiries.",500,undefined,"TRAVEL_INQUIRIES_LOAD_FAILED")}}