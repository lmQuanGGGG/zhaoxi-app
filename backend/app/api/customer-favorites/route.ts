import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {customerPersonalizationService as s} from "@/lib/services/customer-personalization-service";export async function GET(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="customer")return failure("Customer required",401);const u=new URL(r.url);return success(await s.favorites(a.userId,u.searchParams.get("locale")||"zh-CN"))}export async function POST(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="customer")return failure("Customer required",401);const b=await r.json();return success(await s.setFavorite(a.userId,String(b.serviceId),Boolean(b.favorite)))}