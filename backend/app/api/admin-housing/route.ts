import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {housingAnalyticsService} from "@/lib/services/housing-analytics-service";export const dynamic="force-dynamic";
export async function GET(request:Request){const s=await authenticatedSession(request);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");const raw=Number(new URL(request.url).searchParams.get("days")||30),period=(raw===7||raw===90?raw:30) as 7|30|90;try{return success(await housingAnalyticsService.overview({period}))}catch(e){console.error(e);return failure("Unable to load Housing oversight.",500,undefined,"ADMIN_HOUSING_LOAD_FAILED")}}