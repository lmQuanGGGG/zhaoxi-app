import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {restaurantAnalyticsService} from "@/lib/services/restaurant-analytics-service";export const dynamic="force-dynamic";
export async function GET(request:Request){const s=await authenticatedSession(request);if(!s||s.role!=="partner")return failure("Partner required.",403,undefined,"PARTNER_REQUIRED");const u=new URL(request.url),organizationId=u.searchParams.get("organizationId")||s.organizationId||"",raw=Number(u.searchParams.get("days")||30),days=(raw===7||raw===90?raw:30) as 7|30|90,timeZone=String(u.searchParams.get("timezone")||"Asia/Ho_Chi_Minh");if(!organizationId)return failure("organizationId required.",422);try{return success(await restaurantAnalyticsService.overview(s.userId,organizationId,days,timeZone))}catch(e){const code=e instanceof Error?e.message:"RESTAURANT_ANALYTICS_FAILED";return failure("Unable to load restaurant analytics.",code==="PARTNER_FORBIDDEN"?403:500,undefined,code)}}
