import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {customerLocationService,validPoint} from "@/lib/services/customer-location-service";export const dynamic="force-dynamic";
export async function GET(request:Request){const s=await authenticatedSession(request);const u=new URL(request.url),current=validPoint(u.searchParams.get("lat"),u.searchParams.get("lng"));try{return success(await customerLocationService.context(s?.role==="customer"?s.userId:undefined,current))}catch(e){console.error(e);return failure("Unable to load location context.",500,undefined,"LOCATION_CONTEXT_FAILED")}}
