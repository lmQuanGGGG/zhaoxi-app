import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{nativeCompletionContract,parseWorkspaceQuery}from"@/lib/core/mobile-workspace-contract";export const dynamic="force-dynamic";export async function GET(r:Request){const s=await authenticatedSession(r);if(!s||!["customer","partner","admin"].includes(s.role))return failure("Authentication required.",401,undefined,"AUTH_REQUIRED");return success({version:"18.0.5",role:s.role,query:parseWorkspaceQuery(r.url),completion:nativeCompletionContract()})}