import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {roleSwitchService} from "@/lib/services/role-switch-service";export const dynamic="force-dynamic";export async function POST(request:Request){const session=await authenticatedSession(request);if(!session)return failure("Authentication required.",401,undefined,"AUTH_REQUIRED");try{const body=await request.json().catch(()=>({}));return success(await roleSwitchService.create(session.userId,body?.targetRole));}catch(error){const code=error instanceof Error?error.message:"ROLE_SWITCH_CREATE_FAILED";return failure("Unable to create role switch.",code.includes("NOT_")?403:422,undefined,code);}}