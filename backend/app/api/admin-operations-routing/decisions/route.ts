import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{adminOperationsRoutingService as s}from"@/lib/services/admin-operations-routing-service";export async function GET(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="admin")return failure("Admin required.",403);return success(await s.history())}export async function POST(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="admin")return failure("Admin required.",403);try{return success(await s.decide(a.userId,await r.json()),{status:201})}catch(e){return failure("Unable to record routing decision.",422,undefined,e instanceof Error?e.message:"ROUTING_DECISION_FAILED")}}
