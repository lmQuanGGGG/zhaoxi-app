import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {accountService} from "@/lib/services/account-service";export const dynamic="force-dynamic";export async function GET(r:Request){const s=await authenticatedSession(r);if(!s)return failure("Authentication required.",401,undefined,"AUTH_REQUIRED");try{return success(await accountService.get(s.userId))}catch(e){console.error(e);return failure("Unable to load account.",500,undefined,"ACCOUNT_LOAD_FAILED")}}export async function PATCH(r:Request){const s=await authenticatedSession(r);if(!s)return failure("Authentication required.",401,undefined,"AUTH_REQUIRED");try{return success(await accountService.update(s.userId,await r.json().catch(()=>({}))))}catch(e){console.error(e);return failure("Unable to update account.",500,undefined,"ACCOUNT_UPDATE_FAILED")}}