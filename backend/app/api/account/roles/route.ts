import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {accountService} from "@/lib/services/account-service";export const dynamic="force-dynamic";export async function GET(r:Request){const s=await authenticatedSession(r);if(!s)return failure("Authentication required.",401,undefined,"AUTH_REQUIRED");const a=await accountService.get(s.userId);return success({roles:a.roles,organizations:a.organizations,driver:a.driver,betaAccess:a.betaAccess})}