import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{paymentSupportAnalyticsService}from"@/lib/services/payment-support-analytics-service";export const dynamic="force-dynamic";export async function GET(r:Request){const s=await authenticatedSession(r);if(!s||s.role!=="partner")return failure("Partner required.",403,undefined,"PARTNER_REQUIRED");const o=String(new URL(r.url).searchParams.get("organizationId")||s.organizationId||"");try{return success(await paymentSupportAnalyticsService.partner(s.userId,o))}catch(e){const c=e instanceof Error?e.message:"PAYMENT_SUPPORT_ANALYTICS_FAILED";return failure("Unable to load Partner payment support analytics.",c==="PARTNER_FORBIDDEN"?403:422,undefined,c)}}