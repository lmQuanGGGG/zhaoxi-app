import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {releaseAuditService} from "@/lib/services/release-audit-service";export const dynamic="force-dynamic";export async function GET(r:Request){const s=await authenticatedSession(r);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");const u=new URL(r.url);return success(await releaseAuditService.list({resourceType:u.searchParams.get("resourceType")||undefined,app:u.searchParams.get("app")||undefined,actorUserId:u.searchParams.get("actorUserId")||undefined,hours:Number(u.searchParams.get("hours")||168),limit:Number(u.searchParams.get("limit")||100)}))}