import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{restaurantSettlementService}from"@/lib/services/restaurant-settlement-service";export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){const s=await authenticatedSession(r);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");try{const{id}=await params,b=await r.json();return success(await restaurantSettlementService.addAdjustment(s.userId,id,b.amount,b.reason))}catch(e){const c=e instanceof Error?e.message:"SETTLEMENT_ADJUSTMENT_FAILED";return failure("Unable to add adjustment.",422,undefined,c)}}
