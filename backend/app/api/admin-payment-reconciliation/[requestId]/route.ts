import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {partnerPaymentTransactionService} from "@/lib/services/partner-payment-transaction-service";export const dynamic="force-dynamic";
export async function POST(request:Request,{params}:{params:Promise<{requestId:string}>}){const s=await authenticatedSession(request);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");try{const{requestId}=await params;return success(await partnerPaymentTransactionService.reconcile(requestId,s.userId,"admin"))}catch(e){const code=e instanceof Error?e.message:"PAYMENT_RECONCILIATION_FAILED";return failure("Unable to reconcile payment.",code==="TRAVEL_BOOKING_NOT_FOUND"?404:422,undefined,code)}}