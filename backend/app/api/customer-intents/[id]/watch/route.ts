import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{savedSearchWatchService as s}from"@/lib/services/saved-search-watch-service";export async function PATCH(r:Request,{params}:{params:Promise<{id:string}>}){const a=await authenticatedSession(r);if(!a||a.role!=="customer")return failure("Customer required.",401,undefined,"CUSTOMER_REQUIRED");try{const{id}=await params,b=await r.json(),locale=String(b.locale||"vi-VN");return success(await s.setWatch(a.userId,id,b.enabled===true,locale))}catch(e){return failure("Unable to update saved search watch.",422,undefined,e instanceof Error?e.message:"SAVED_SEARCH_WATCH_FAILED")}}