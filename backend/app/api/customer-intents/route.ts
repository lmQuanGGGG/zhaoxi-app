import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{customerIntentMemoryService as s}from"@/lib/services/customer-intent-memory-service";export const dynamic="force-dynamic";export async function GET(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="customer")return failure("Customer required.",401,undefined,"CUSTOMER_REQUIRED");return success(await s.list(a.userId))}export async function POST(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="customer")return failure("Customer required.",401,undefined,"CUSTOMER_REQUIRED");try{return success(await s.create(a.userId,await r.json()),{status:201})}catch(e){return failure("Unable to save search.",422,undefined,e instanceof Error?e.message:"SAVED_SEARCH_FAILED")}}