import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{customerUnifiedUnreadService as s}from"@/lib/services/customer-unified-unread-service";export const dynamic="force-dynamic";export async function GET(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="customer")return failure("Customer required.",401);return success(await s.summary(a.userId,new URL(r.url).searchParams.get("locale")||"vi-VN"))}