import {failure,success} from "@/lib/core/api-response";import {authenticatedSession} from "@/lib/auth-request";import {betaAccessService} from "@/lib/services/beta-access-service";export const dynamic="force-dynamic";export async function GET(request:Request){const s=await authenticatedSession(request);if(!s)return failure("Authentication required.",401);return success(await betaAccessService.check(s.userId,s.role))}
