import {failure,success} from "@/lib/core/api-response";import {authenticatedSession} from "@/lib/auth-request";import {betaAccessService} from "@/lib/services/beta-access-service";export async function PATCH(r:Request,{params}:{params:Promise<{id:string}>}){try{const s=await authenticatedSession(r);if(s?.role!=="admin")return failure("Admin access required.",403);const{id}=await params;const b=await r.json();return success(await betaAccessService.setInviteStatus(id,String(b.status||"suspended")))}catch(e){return failure(e instanceof Error?e.message:"Unable to update invite.",422)}}
