import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{partnerQualityPolicyService}from"@/lib/services/partner-quality-policy-service";export const dynamic="force-dynamic";export async function GET(r:Request){const s=await authenticatedSession(r);if(!s||s.role!=="partner")return failure("Partner required.",403,undefined,"PARTNER_REQUIRED");const o=String(new URL(r.url).searchParams.get("organizationId")||s.organizationId||"");try{return success(await partnerQualityPolicyService.partner(s.userId,o))}catch(e){return failure("Unable to load Partner quality.",403,undefined,e instanceof Error?e.message:"PARTNER_QUALITY_FAILED")}}