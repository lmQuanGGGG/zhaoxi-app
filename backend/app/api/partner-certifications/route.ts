import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{partnerTrustGovernanceService as s}from"@/lib/services/partner-trust-governance-service";export const dynamic="force-dynamic";export async function GET(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="partner")return failure("Partner required.",403);const o=String(new URL(r.url).searchParams.get("organizationId")||a.organizationId||"");try{return success(await s.partner(a.userId,o))}catch(e){return failure("Unable to load certifications.",403,undefined,e instanceof Error?e.message:"CERTIFICATIONS_FAILED")}}