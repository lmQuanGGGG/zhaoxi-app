import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {releaseHealthService} from "@/lib/services/release-health-service";export const dynamic="force-dynamic";export async function GET(r:Request){const s=await authenticatedSession(r);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");const u=new URL(r.url);return success(await releaseHealthService.snapshot(Number(u.searchParams.get("minutes")||60)))}