import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {releaseAlertService} from "@/lib/services/release-alert-service";export const dynamic="force-dynamic";export async function PATCH(r:Request,{params}:{params:Promise<{id:string}>}){const s=await authenticatedSession(r);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");try{const{id}=await params;return success(await releaseAlertService.updatePolicy(id,await r.json().catch(()=>({})),s.userId))}catch(e){const c=e instanceof Error?e.message:"ALERT_POLICY_UPDATE_FAILED";return failure("Unable to update alert policy.",422,undefined,c)}}