import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {releaseAlertService} from "@/lib/services/release-alert-service";export const dynamic="force-dynamic";export async function GET(r:Request){const s=await authenticatedSession(r);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");return success(await releaseAlertService.listPolicies())}export async function POST(r:Request){const s=await authenticatedSession(r);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");try{return success(await releaseAlertService.createPolicy(await r.json().catch(()=>({})),s.userId),{status:201})}catch(e){const c=e instanceof Error?e.message:"ALERT_POLICY_CREATE_FAILED";return failure("Unable to create alert policy.",422,undefined,c)}}