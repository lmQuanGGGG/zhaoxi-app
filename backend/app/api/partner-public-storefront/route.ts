import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{partnerPublicStorefrontService as s}from"@/lib/services/partner-public-storefront-service";export const dynamic="force-dynamic";export async function GET(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="partner")return failure("Partner required.",403);const o=String(new URL(r.url).searchParams.get("organizationId")||a.organizationId||"");try{return success(await s.get(a.userId,o))}catch(e){return failure("Unable to load storefront settings.",403,undefined,e instanceof Error?e.message:"STOREFRONT_FAILED")}}export async function PATCH(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="partner")return failure("Partner required.",403);try{const body=await r.json(),o=String(body.organizationId||a.organizationId||"");return success(await s.update(a.userId,o,body))}catch(e){return failure("Unable to update storefront.",422,undefined,e instanceof Error?e.message:"STOREFRONT_UPDATE_FAILED")}}