import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {featureFlagService} from "@/lib/services/feature-flag-service";export const dynamic="force-dynamic";export async function PATCH(r:Request,{params}:{params:Promise<{id:string}>}){const s=await authenticatedSession(r);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");try{const{id}=await params;return success(await featureFlagService.update(id,await r.json().catch(()=>({})),s.userId))}catch(e){const c=e instanceof Error?e.message:"FEATURE_UPDATE_FAILED";return failure("Unable to update feature flag.",422,undefined,c)}}