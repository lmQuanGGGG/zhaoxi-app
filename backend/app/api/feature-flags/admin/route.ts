import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {featureFlagService} from "@/lib/services/feature-flag-service";export const dynamic="force-dynamic";export async function GET(r:Request){const s=await authenticatedSession(r);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");return success(await featureFlagService.list())}export async function POST(r:Request){const s=await authenticatedSession(r);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");try{return success(await featureFlagService.create(await r.json().catch(()=>({})),s.userId),{status:201})}catch(e){const c=e instanceof Error?e.message:"FEATURE_CREATE_FAILED";return failure("Unable to create feature flag.",422,undefined,c)}}