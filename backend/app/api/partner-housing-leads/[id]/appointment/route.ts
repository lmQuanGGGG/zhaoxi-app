import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {housingAppointmentService} from "@/lib/services/housing-appointment-service";export const dynamic="force-dynamic";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const s=await authenticatedSession(request);if(!s||s.role!=="partner")return failure("Partner required.",403,undefined,"PARTNER_REQUIRED");try{const{id}=await params,body=await request.json(),organizationId=String(body.organizationId||s.organizationId||"");return success(await housingAppointmentService.partnerAction(s.userId,organizationId,id,body))}catch(e){const code=e instanceof Error?e.message:"HOUSING_APPOINTMENT_UPDATE_FAILED";const status=code==="PARTNER_FORBIDDEN"?403:422;return failure("Unable to update housing appointment.",status,undefined,code)}}
