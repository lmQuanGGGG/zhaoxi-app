import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {housingLeadService} from "@/lib/services/housing-lead-service";export const dynamic="force-dynamic";
export async function GET(request:Request){const s=await authenticatedSession(request);if(!s||s.role!=="partner")return failure("Partner required.",403,undefined,"PARTNER_REQUIRED");const organizationId=new URL(request.url).searchParams.get("organizationId")||s.organizationId||"";try{return success(await housingLeadService.listPartner(s.userId,organizationId))}catch(e){const code=e instanceof Error?e.message:"HOUSING_LEADS_FAILED";return failure("Unable to load housing leads.",code==="PARTNER_FORBIDDEN"?403:500,undefined,code)}}
