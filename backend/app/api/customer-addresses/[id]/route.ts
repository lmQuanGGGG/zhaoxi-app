import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {customerProfileService} from "@/lib/services/customer-profile-service";
export async function PATCH(r:Request,{params}:{params:Promise<{id:string}>}){const s=await authenticatedSession(r);if(!s||s.role!=="customer")return failure("Customer authentication required.",401,undefined,"CUSTOMER_AUTH_REQUIRED");const{id}=await params;try{return success(await customerProfileService.setDefault(s.userId,id))}catch(e){console.error(e);return failure("Unable to update address.",422,undefined,"ADDRESS_UPDATE_FAILED")}}
export async function DELETE(r:Request,{params}:{params:Promise<{id:string}>}){const s=await authenticatedSession(r);if(!s||s.role!=="customer")return failure("Customer authentication required.",401,undefined,"CUSTOMER_AUTH_REQUIRED");const{id}=await params;try{return success(await customerProfileService.removeAddress(s.userId,id))}catch(e){console.error(e);return failure("Unable to delete address.",422,undefined,"ADDRESS_DELETE_FAILED")}}
