import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{customerNotificationCenterService as s}from"@/lib/services/customer-notification-center-service";export const dynamic="force-dynamic";export async function GET(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="customer")return failure("Customer required.",401);const u=new URL(r.url);return success(await s.inbox(a.userId,u.searchParams.get("locale")||"vi-VN",u.searchParams.get("filter")||"all"))}