import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {travelInventoryService} from "@/lib/services/travel-inventory-service";export const dynamic="force-dynamic";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const s=await authenticatedSession(request);if(!s||s.role!=="partner")return failure("Partner required.",403,undefined,"PARTNER_REQUIRED");try{const{id}=await params;return success(await travelInventoryService.update(s.userId,id,await request.json()))}catch(e){const code=e instanceof Error?e.message:"TRAVEL_INVENTORY_UPDATE_FAILED";return failure("Unable to update Travel experience.",code==="PARTNER_FORBIDDEN"?403:422,undefined,code)}}