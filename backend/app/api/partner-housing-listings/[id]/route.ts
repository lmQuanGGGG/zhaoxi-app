import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {housingInventoryService} from "@/lib/services/housing-inventory-service";export const dynamic="force-dynamic";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const s=await authenticatedSession(request);if(!s||s.role!=="partner")return failure("Partner required.",403,undefined,"PARTNER_REQUIRED");try{const{id}=await params;return success(await housingInventoryService.update(s.userId,id,await request.json()))}catch(e){const code=e instanceof Error?e.message:"HOUSING_LISTING_UPDATE_FAILED";return failure("Unable to update Housing listing.",code==="PARTNER_FORBIDDEN"?403:422,undefined,code)}}
export async function DELETE(request:Request,{params}:{params:Promise<{id:string}>}){const s=await authenticatedSession(request);if(!s||s.role!=="partner")return failure("Partner required.",403,undefined,"PARTNER_REQUIRED");try{const{id}=await params;return success(await housingInventoryService.archive(s.userId,id))}catch(e){const code=e instanceof Error?e.message:"HOUSING_LISTING_ARCHIVE_FAILED";return failure("Unable to archive Housing listing.",code==="PARTNER_FORBIDDEN"?403:422,undefined,code)}}