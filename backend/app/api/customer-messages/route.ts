import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{customerMessageCenterService as s}from"@/lib/services/customer-message-center-service";export const dynamic="force-dynamic";export async function GET(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="customer")return failure("Customer required.",401);return success(await s.inbox(a.userId,new URL(r.url).searchParams.get("locale")||"vi-VN"))}export async function POST(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="customer")return failure("Customer required.",401);try{return success(await s.createSupport(a.userId,await r.json()),{status:201})}catch(e){return failure("Unable to create support thread.",422,undefined,e instanceof Error?e.message:"SUPPORT_THREAD_FAILED")}}