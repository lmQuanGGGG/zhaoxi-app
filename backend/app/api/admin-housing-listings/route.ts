import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {housingModerationService} from "@/lib/services/housing-moderation-service";export const dynamic="force-dynamic";
export async function GET(request:Request){const s=await authenticatedSession(request);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");const locale=new URL(request.url).searchParams.get("locale")||"zh-CN";try{return success(await housingModerationService.list(locale))}catch(e){console.error(e);return failure("Unable to load Housing moderation.",500,undefined,"HOUSING_MODERATION_LOAD_FAILED")}}