import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {housingModerationService} from "@/lib/services/housing-moderation-service";export const dynamic="force-dynamic";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){const s=await authenticatedSession(request);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");try{const{id}=await params;return success(await housingModerationService.act(s.userId,id,await request.json()))}catch(e){const code=e instanceof Error?e.message:"HOUSING_MODERATION_UPDATE_FAILED";return failure("Unable to update Housing moderation.",code==="HOUSING_LISTING_NOT_FOUND"?404:422,undefined,code)}}