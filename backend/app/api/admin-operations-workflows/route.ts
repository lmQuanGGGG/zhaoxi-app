import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{adminOperationsWorkflowService as s}from"@/lib/services/admin-operations-workflow-service";export const dynamic="force-dynamic";export async function GET(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="admin")return failure("Admin required.",403);return success(await s.center())}export async function POST(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="admin")return failure("Admin required.",403);try{return success(await s.propose(a.userId,await r.json()),{status:201})}catch(e){return failure("Unable to propose workflow.",422,undefined,e instanceof Error?e.message:"WORKFLOW_PROPOSE_FAILED")}}
