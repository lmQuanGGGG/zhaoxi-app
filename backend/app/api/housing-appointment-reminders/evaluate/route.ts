import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {housingAppointmentReminderService} from "@/lib/services/housing-appointment-reminder-service";export const dynamic="force-dynamic";
export async function POST(request:Request){const s=await authenticatedSession(request);if(!s||!["admin","partner"].includes(s.role))return failure("Authorized role required.",403,undefined,"ROLE_REQUIRED");try{return success(await housingAppointmentReminderService.evaluate())}catch(e){console.error(e);return failure("Unable to evaluate Housing appointment reminders.",500,undefined,"HOUSING_REMINDER_EVALUATE_FAILED")}}