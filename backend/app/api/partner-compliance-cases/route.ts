import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{partnerComplianceCaseService as s}from"@/lib/services/partner-compliance-case-service";export async function GET(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="partner")return failure("Partner required.",403);const o=String(new URL(r.url).searchParams.get("organizationId")||a.organizationId||"");try{return success(await s.partner(a.userId,o))}catch(e){return failure("Unable to load compliance cases.",403,undefined,e instanceof Error?e.message:"CASE_LIST_FAILED")}}