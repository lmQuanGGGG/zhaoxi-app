import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {restaurantCommissionPolicyService} from "@/lib/services/restaurant-commission-policy-service";export const dynamic="force-dynamic";
export async function GET(r:Request){const s=await authenticatedSession(r);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");return success(await restaurantCommissionPolicyService.list())}
export async function PUT(r:Request){const s=await authenticatedSession(r);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");try{return success(await restaurantCommissionPolicyService.save(s.userId,await r.json()))}catch(e){const code=e instanceof Error?e.message:"COMMISSION_POLICY_SAVE_FAILED";return failure("Unable to save commission policy.",422,undefined,code)}}
