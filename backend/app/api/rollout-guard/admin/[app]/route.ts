import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {rolloutGuardService} from "@/lib/services/rollout-guard-service";export const dynamic="force-dynamic";export async function PATCH(r:Request,{params}:{params:Promise<{app:string}>}){const s=await authenticatedSession(r);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");try{const{app}=await params;return success(await rolloutGuardService.updatePolicy(app,await r.json().catch(()=>({})),s.userId))}catch(e){const c=e instanceof Error?e.message:"ROLLOUT_GUARD_UPDATE_FAILED";return failure("Unable to update rollout guard.",422,undefined,c)}}