import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{paymentOperationService}from"@/lib/services/payment-operation-service";export const dynamic="force-dynamic";export async function GET(r:Request){const s=await authenticatedSession(r);if(!s||s.role!=="partner")return failure("Partner required.",403,undefined,"PARTNER_REQUIRED");const o=String(new URL(r.url).searchParams.get("organizationId")||s.organizationId||"");try{await paymentOperationService.partner(s.userId,o);return success(await paymentOperationService.list(o))}catch(e){return failure("Unable to load payment operations.",403,undefined,e instanceof Error?e.message:"PAYMENT_OPERATIONS_FAILED")}}