import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {adminTravelOversightService} from "@/lib/services/admin-travel-oversight-service";export const dynamic="force-dynamic";
export async function GET(request:Request){const s=await authenticatedSession(request);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");const u=new URL(request.url),raw=Number(u.searchParams.get("days")||30),period=(raw===7||raw===90?raw:30) as 7|30|90,locale=u.searchParams.get("locale")||"zh-CN";try{return success(await adminTravelOversightService.overview(period,locale))}catch(e){console.error(e);return failure("Unable to load Travel oversight.",500,undefined,"ADMIN_TRAVEL_LOAD_FAILED")}}