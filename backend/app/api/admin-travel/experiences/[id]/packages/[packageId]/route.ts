import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {adminTravelOversightService} from "@/lib/services/admin-travel-oversight-service";export const dynamic="force-dynamic";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string;packageId:string}>}){const s=await authenticatedSession(request);if(!s||s.role!=="admin")return failure("Admin required.",403,undefined,"ADMIN_REQUIRED");try{const{id,packageId}=await params;return success(await adminTravelOversightService.moderatePackage(s.userId,id,packageId,await request.json()))}catch(e){const code=e instanceof Error?e.message:"TRAVEL_PACKAGE_MODERATION_FAILED";return failure("Unable to moderate Travel package.",code==="TRAVEL_PACKAGE_NOT_FOUND"?404:422,undefined,code)}}