import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{customerHomePersonalizationService as s}from"@/lib/services/customer-home-personalization-service";export const dynamic="force-dynamic";export async function GET(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="customer")return failure("Customer required.",401,undefined,"CUSTOMER_REQUIRED");const locale=new URL(r.url).searchParams.get("locale")||"vi-VN";return success(await s.feed(a.userId,locale))}