import {authenticatedSession} from "@/lib/auth-request";import {failure,success} from "@/lib/core/api-response";import {travelPlatformFeeService} from "@/lib/services/travel-platform-fee-service";export const dynamic="force-dynamic";
export async function GET(request:Request){const s=await authenticatedSession(request);if(!s||s.role!=="partner")return failure("Partner required.",403,undefined,"PARTNER_REQUIRED");const organizationId=String(new URL(request.url).searchParams.get("organizationId")||s.organizationId||"");try{return success(await travelPlatformFeeService.partnerOverview(s.userId,organizationId))}catch(e){const code=e instanceof Error?e.message:"TRAVEL_PLATFORM_FEE_LOAD_FAILED";return failure("Unable to load Travel platform fee overview.",code==="PARTNER_FORBIDDEN"?403:422,undefined,code)}}