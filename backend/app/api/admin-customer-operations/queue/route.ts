import{authenticatedSession}from"@/lib/auth-request";import{failure,success}from"@/lib/core/api-response";import{adminCustomerOperationsService as s}from"@/lib/services/admin-customer-operations-service";export const dynamic="force-dynamic";export async function GET(r:Request){const a=await authenticatedSession(r);if(!a||a.role!=="admin")return failure("Admin required.",403);const u=new URL(r.url);return success(await s.queue({status:u.searchParams.get("status")||undefined,assignee:u.searchParams.get("assignee")||undefined,priority:u.searchParams.get("priority")||undefined}))}