import { authenticatedSession } from "@/lib/auth-request"; import { failure, success } from "@/lib/core/api-response"; import { driverService } from "@/lib/services/driver-service"; export const dynamic="force-dynamic";
export async function GET(request:Request){const session=await authenticatedSession(request);if(!session||session.role!=="driver")return failure("Driver authentication required.",401,undefined,"DRIVER_AUTH_REQUIRED");return success(await driverService.ensureProfile(session.userId));}
export async function PATCH(request:Request){const session=await authenticatedSession(request);if(!session||session.role!=="driver")return failure("Driver authentication required.",401,undefined,"DRIVER_AUTH_REQUIRED");const body=await request.json().catch(()=>({}));try{return success(await driverService.updateProfile(session.userId,{status:body.status,phone:body.phone,vehicleType:body.vehicleType,plateNumber:body.plateNumber}));}catch(e){console.error(e);return failure("Unable to update driver profile.",500,undefined,"DRIVER_PROFILE_UPDATE_FAILED");}}
