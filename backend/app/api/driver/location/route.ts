import { authenticatedSession } from "@/lib/auth-request"; import { failure, success } from "@/lib/core/api-response"; import { driverService } from "@/lib/services/driver-service"; export const dynamic="force-dynamic";
export async function PUT(request:Request){const session=await authenticatedSession(request);if(!session||session.role!=="driver")return failure("Driver authentication required.",401,undefined,"DRIVER_AUTH_REQUIRED");const body=await request.json().catch(()=>({}));const latitude=Number(body.latitude),longitude=Number(body.longitude);if(!Number.isFinite(latitude)||!Number.isFinite(longitude))return failure("Valid coordinates are required.",422,undefined,"LOCATION_REQUIRED");return success(await driverService.updateLocation(session.userId,{latitude,longitude,accuracyMeters:Number.isFinite(Number(body.accuracyMeters))?Number(body.accuracyMeters):undefined,heading:Number.isFinite(Number(body.heading))?Number(body.heading):undefined,speedMps:Number.isFinite(Number(body.speedMps))?Number(body.speedMps):undefined}));}
